import React from 'react'

export default function Label(props) {
  return (
        <div className="first">
    
        <span className="heading">{props.title}</span>
        </div>
    
    
  );
}

